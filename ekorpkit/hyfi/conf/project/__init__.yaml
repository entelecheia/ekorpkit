defaults:
  - /dotenv: __init__
  - /joblib: __init__
  - /path: __init__

project_name: ${oc.env:HYFI_PROJECT_NAME,hyfi-project}
task_name: ${oc.env:HYFI_TASK_NAME,default-task}
project_root: ${.path.project}
workspace_root: ${alt:${oc.env:HYFI_WORKSPACE_ROOT,null},${.path.home}/.hyfi}
description:
use_huggingface_hub: false
use_wandb: false
verbose: false
