defaults:
  - pysbd

merge:
  merge_lines: true
  merge_level: segment
  empty_lines_threshold: 0.6
  broken_lines_threshold: 0.4
filter_language:
  filter: true
  detection_level: segment
  languages_to_keep:
    - en
    - ko
