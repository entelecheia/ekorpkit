defaults:
  - dataset_simple_classification

prefix: ${oc.select:.._keys_.prefix,prefix}
input_text: ${.text}
target_text: ${.labels}
input: ${.input_text}
actual: ${.target_text}
predicted: pred_labels
model_outputs: raw_preds
