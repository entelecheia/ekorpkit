defaults:
  - default
  - /info: _build_dataset

_target_: ekorpkit.datasets.datasets.Datasets
name: 
datasets:
data_dir: ${dir.dataset}/dataset
autorun:
  - load
  # - name: concatenate
  #   args:
  #     append_dataset_name: true
