defaults:
  - feature
  - /pipeline: _build_feature
  - /info: _build_feature

autobuild: true
force_rebuild: false
info:
  data_dir: ${iif:${..use_name_as_subdir},${..data_dir}/${.name},${..data_dir}}
pipeline:
  name: ${oc.select:..name, null}
  output_dir: ${iif:${..use_name_as_subdir},${..data_dir}/${.name},${..data_dir}}
  split_sampling:
    dev_size:
    test_size: 0.2
