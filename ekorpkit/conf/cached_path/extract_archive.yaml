uri:
extract_archive: true
force_extract: false
return_parent_dir: true
persist_cache: false
persistent_dir: ${dir.data}
cache_dir: ${dir.cache}
verbose: ${oc.select:..verbose, false}
path: ${cached_path:${.uri},${.extract_archive},${.force_extract},${.return_parent_dir},${.persist_cache},${.persistent_dir},${.cache_dir},${.verbose}}
