# @package _global_
defaults:
  - /corpus/builtin: bok_minutes
  - override /cmd: fetch_builtin_corpus

corpus:
  builtin:
    io:
      data_dir: ${cached_path:'https://github.com/entelecheia/ekorpkit-config/raw/main/data/bok_minutes_raw.zip',true,false}/bok_minutes_raw
