defaults:
  - /fetch: corpus
  - /info: corpus_build
  - /info/stat: corpus
  - /pipeline: corpus_build_pipeline
  - override /cmd@_global_: fetch_corpus

name: 
data_dir: ${dir.corpus}/${.name}
# data_dir: ${dir.archive}/datasets/lmdata/${.name}
filetype: .parquet
segment_separator: \n\n
sentence_separator: \n
verbose: ${oc.select:..verbose, true}
