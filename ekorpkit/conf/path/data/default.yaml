file: ${..data_file}
filename: ${.file}
base_dir: ${..data_dir}
filetype: ${..filetype}
concatenate: ${..concat_data}
dype:
parse_dates:
columns: ${..columns}
filepath: ${join_path:${.base_dir},${.filename}}
