name: disco-imagen
root: ${join_path:${oc.select:dir.project,${oc.env:EKORPKIT_PROJECT_DIR,./}},${.name}}
batch_name: demo
library_dir: ${join_path:${.root},libs}
cache_dir: ${join_path:${.root},cache}
model_dir: ${join_path:${.root},models}
output_dir: ${join_path:${.root},outputs}
batch_dir: ${.output_dir}/${.batch_name}
tmp_dir: ${join_path:${.root},tmp}
verbose: ${oc.select:..verbose,false}
