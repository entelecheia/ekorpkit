# @package _global_
defaults:
  - _global_
  - override /mode: debug

project:
  name: _test_
dir:
  workspace: ./data/tmp/.ekorpkit
env:
  os:
    WANDB_API_KEY:
  distributed_framework:
    backend: joblib
