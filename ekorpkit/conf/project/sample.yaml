# @package _global_
defaults:
  - _global_
  - override /mode: debug

project: 
  name: sample-nlp
dir:
  data: /workspace/data/
  project: /workspace/data/${project.name}
  dataset: /workspace/data/datasets
  model: /workspace/data/models
env:
  os:
    WANDB_API_KEY:
  distributed_framework:
    backend: joblib
