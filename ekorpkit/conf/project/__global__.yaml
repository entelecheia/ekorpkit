defaults:
  - /env: default
  - /path: __project__

project_name: ${oc.select:..name,ekorpkit-project}
project_dir: ${.path.project}
task_name: default-task
workspace_root: ${oc.select:..root,${.path.home}/.ekorpkit}
description: 
