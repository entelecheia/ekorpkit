model_type: ${.pretrained.model_type}
model_name: ${.pretrained.model_name}
tokenizer_type:
tokenizer_name:
num_labels:
device: "cuda:-1"
cache_dir: ${..path.cache_dir}/models

model_name_or_path: ${.pretrained.model_name_or_path}
model_class: classification
output_dir: ${..path.model_dir}/${.model_name}/${..name}
overwrite_output_dir: false
log_dir: ${..path.log_dir}
wandb_project: ${..project.project_name}/${..name}
wandb_group: ${.model_name}
