score_dir: ${dir.project}/outputs/ngrams/
score_file: ${..name}_${..score_function.name}_scores.parquet
score_path: ${join_paths:${.score_dir},${.score_file}}
columns:
  words: words
  score: score
ngram_delim: ${..ngram.delimiter}
