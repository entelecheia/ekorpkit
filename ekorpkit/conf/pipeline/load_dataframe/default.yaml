defaults:
  - /_func_: load_dataframe

filepath:
filetype:
data_dir: ${oc.select:..data_dir, ''}
data_file: ${oc.select:..data_file, ''}
path:
  columns:
concatenate: true
dtype:
parse_dates:
verbose: ${oc.select:..verbose, true}
