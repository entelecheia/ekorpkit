defaults:
  - _default
  - /pipeline: default

_target_: ekorpkit.io.fetch.hfds.HFDS
subsets:
splits:
pipeline:
  data_dir: ${..output_dir}
  data_file: ${..output_file}
ignore_verifications: true
download_mode:
