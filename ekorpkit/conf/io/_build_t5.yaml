defaults:
  - loader: tsv

name: ${oc.select:..name, ''}
data_dir: ${dir.archive}/datasets/t5/source/${.name}
data_sources:
  train: 
num_workers: ${oc.select:task.num_workers,100}
force_download: false
overwrite: false
calculate_stats: false
preprocess_text: false
verbose: ${oc.select:verbose, false}